# dependencies & build output
node_modules/
dist/

# local tooling
.claude/

# Windows Explorer thumbnail cache
Thumbs.db

# personal files — PDFs (e.g. the local resume copy) and the internal master
# profile contain private details and must never be pushed to this public repo
*.pdf
Master-Profile.md
HANDOFF.md
